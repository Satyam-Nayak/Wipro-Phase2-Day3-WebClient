server:
  port: 9200
spring:
  application:
    name: Account-Service
  datasource:
    url: jdbc:mysql://localhost:3306/project
    username: root
    password: SatyamN.26.
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true